GITHUB_TOKEN="personal access token"
TASKFILE_URL="https://$GITHUB_TOKEN@raw.githubusercontent.com/$TASKFILE_REPO/$TASKFILE_BRANCH/{{.TASKFILE_REMOTE_NAME}}" # remote name is set within the taskfile
TASKFILE_REPO="user/taskfiles"
TASKFILE_BRANCH="main"